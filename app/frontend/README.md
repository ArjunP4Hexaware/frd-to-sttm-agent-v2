# review_app_react frontend

React + TypeScript + Vite, using `@databricks/appkit-ui` components. See
the root `README.md`'s "React/AppKit review app" section for what this is
and how to run it alongside `../backend`.
