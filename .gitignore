# client documents never enter the repo except sample_documents/ (deliberate, purged on a date)
local_data/
*.xlsx
*.docx
*.pdf
!sample_documents/*
!standards/*.docx
!templates/*
.env
.env.*
!.env.example
.DS_Store
__pycache__/
*.pyc
.venv/
venv/
src/*.egg-info/
.pytest_cache/
.databricks/
.~lock.*#
~$*
app/frontend/node_modules/
# app/frontend/dist is TRACKED: the Databricks workspace serves it without a build step.
# Run `npm run build` before committing a UI change.
